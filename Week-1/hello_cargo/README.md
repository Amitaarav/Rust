## 

- Git files won't be generated if you run ` cargo new` within an existing repository
- can be override this behavior by using `cargo new --vcs=git`
- `cargo init`: inside existing directory and name of dir does not start with number
